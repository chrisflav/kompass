{% extends "members/tex_base.tex" %}
{% load static common tex_extras %}

{% block title %}Teilnehmer*innenliste Sektionsveranstaltung{% endblock %}

{% block content %}

% DESCRIPTION TABLE
\begin{table}[H]
    \begin{tabular}{ll}
        \large Aktivität: & {{ memberlist.name|default:memberlist.title|esc_all }} \\
        \large Ordnungsnummer & {{ memberlist.code|esc_all }} \\
        {% if memberlist.groups_str %}\large Gruppe: & {{ memberlist.groups_str|esc_all }}  \\{% endif %}
        \large Ort / Stützpunkt: & {{ memberlist.place|default:memberlist.location|esc_all }} \\
        {% if memberlist.destination %}\large ggf. Ziel: & {{ memberlist.destination|esc_all }} \\{% endif %}
        \large Zeitraum: & {{ memberlist.time_period_str|esc_all }} \\
        {% if memberlist.staff_str %}\large Betreuer:innen: & {{ memberlist.staff_str|esc_all }} \\{% endif %}
        {% if memberlist.get_tour_type %}\large Art der Tour: & {% checked_if_true 'Gemeinschaftstour' memberlist.get_tour_type %}
            {% checked_if_true 'Führungstour' memberlist.get_tour_type %}
            {% checked_if_true 'Ausbildung' memberlist.get_tour_type %} \\{% endif %}
        {% if memberlist.get_tour_approach %}\large Anreise: & {% checked_if_true 'ÖPNV' memberlist.get_tour_approach %}
            {% checked_if_true 'Muskelkraft' memberlist.get_tour_approach %}
            {% checked_if_true 'Fahrgemeinschaften' memberlist.get_tour_approach %}{% endif %}
    \end{tabular}
\end{table}

\begin{tabularx}{.97\linewidth}{lXXXX}
    \toprule
    \# & \textbf{Name} & \textbf{Anschrift} & \textbf{Telefon} & \textbf{Notfallkontakte} \\
    \midrule
    \endhead
    \bottomrule
    \endfoot

    {% for m in memberlist.membersonlist.all %}
        {{ forloop.counter }} &
        {{ m.member.name|esc_all }} &
        {{ m.member.address_multiline|esc_all }} &
        {{ m.member.contact_phone_number|esc_all }} &
        {% for c in m.member.emergencycontact_set.all %}
            {{ c.name|esc_all }} \newline
            Tel.: {{ c.phone_number|esc_all }}
        {% endfor %} \\
    {% endfor %}

\end{tabularx}

\vspace{1cm}

\noindent Bitte die ausgefüllte Teilnehmerliste vor Antritt der Aktivität per E-Mail an
\href{mailto:{{ settings.SEKTION_CRISIS_INTERVENTION_MAIL }}}{ {{ settings.SEKTION_CRISIS_INTERVENTION_MAIL }} } senden.

{% endblock content %}
